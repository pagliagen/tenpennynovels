/**
 * Types for Documents Module
 *
 * Shared types and interfaces for documents module.
 * Most types are defined inline in services/controllers.
 */

// Re-export RouteWithDocument from RouteService
export type { RouteWithDocument } from '../services/RouteService';
